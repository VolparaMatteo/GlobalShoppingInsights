// ---------------------------------------------------------------------------
// DEPRECATED wrapper — usa @/components/common/StatusBadge direttamente.
// Mantenuto per backward-compatibility finché non facciamo il rename negli
// import esistenti (pianificato nel batch refactoring di Sprint 7).
// ---------------------------------------------------------------------------
export { default } from '@/components/common/StatusBadge';
