# ============================================================================
# GSI Frontend — configurazione d'ambiente
# Copia in `.env.local` per override personalizzati (non committati).
# ============================================================================

# Base URL dell'API backend (include /api/v1)
VITE_API_BASE_URL=http://localhost:8000/api/v1
